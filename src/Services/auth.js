// Authentication service to handle login and registration

// src/services/auth.js
export const login = (email, password) => {
    // Placeholder function for login (integrate with backend API)
    if (email === 'user@domain.com' && password === 'password123') {
      return { token: 'fake-jwt-token' }; // Mocked response
    }
};  